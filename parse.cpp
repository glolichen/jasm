#include <vector>
#include <string>

#include "parse.hpp"
#include "lex.hpp"

void parse::parse(std::vector<parse::Statement> &stmts, const std::vector<std::vector<lex::Lexeme>> &tokens) {
	// for (int i = 0)

}

